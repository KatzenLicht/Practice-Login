En este repositorio se creara una API para inciar sesion de un solo usuario
hacemos la instalación NPM:
    npm install jsonwebtoken
Agregamos la libreria a la API
    const jwt = require("jsonwebtoken")
